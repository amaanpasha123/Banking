'use server';

export const signIn = async ()=>{
    try{//mutation / Database / Make Fetch

    }catch( error ){
        console.error('Error', error);
    }

}

export const signUp = async (userData : SignUpParams)=>{
    try{//mutation / Database / Make Fetch

    }catch( error ){
        console.error('Error', error);
    }
}

